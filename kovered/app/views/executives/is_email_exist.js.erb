<% if @user.present? %>
if($("#executive_user_attributes_email").siblings('p').length != 1) {
    $("<p />", {
        text: "Someone already has that Email. Try another?",
        class: 'is_email_exist'
    }).insertAfter("#executive_user_attributes_email").parents('.form-group').addClass('field_with_errors');
}
<% else %>
    $("#executive_user_attributes_email").next('p.is_email_exist').remove();
    $("#executive_user_attributes_email").parents('.form-group').removeClass('field_with_errors')
<% end %>
