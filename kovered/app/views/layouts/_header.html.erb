<nav class="navbar navbar-default top-nav navbar-fixed-top" role="navigation">

  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle btn btn-default" data-toggle="collapse" data-target="#navbar-fixed-layout-collapse">
        <i class="fa fa-indent"></i>
      </button>
      <%= link_to root_path, :class=>"navbar-brand" do %>
        <%= image_tag "logo/logo.jpg", size: "46x45", alt: "Go Hom" %>
      <% end %>
      <!--<a class="navbar-brand" href="index.html"><img src="/assets/logo/logo.jpg" alt=""></a>-->
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-fixed-layout-collapse">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <%= link_to 'Discover <b class="caret"></b>'.html_safe, root_path, :class => 'dropdown-toggle', :data => {:toggle=>"dropdown"} %>
          <!--<a href="#" class="dropdown-toggle" data-toggle="dropdown">Discover <b class="caret"></b></a>-->
          <ul class="dropdown-menu">
            <li>
              <%= link_to 'Artists', root_path %>
            </li>
            <li>
              <%= link_to 'Songs', root_path %>
            </li>
          </ul>
        </li>
      </ul>
      <form class="navbar-form navbar-left hidden-sm" role="search">
        <div class="form-group inline-block">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-inverse"><i class="fa fa-search fa-fw"></i></button>
      </form>
      <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <% if current_user.artist %>
            <li class="innerLR">
              <%= link_to new_video_path do%>
                  <button type="button" class="btn btn-success2 navbar-btn"><i class="fa fa-cloud-upload"></i> Upload Video</button>
              <% end %>
            </li>
            <li><%= link_to videos_path do %><i class='fa fa-video-camera fa-fw'></i> My Videos<% end %></li>
      <% end %>
        <li class="buttondivider"></li>
          <li class="dropdown">
            <%= link_to root_path, :class => 'dropdown-toggle', :data => {:toggle=>"dropdown"} do %>
              <span class="pull-left innerR">
                <%#=
                    if current_user.avatar.present?
                      image_tag(current_user.avatar.thumb ) unless current_user.nil?
                    else
                      image_tag("default_student.png")
                    end
                %>
                <%= image_tag 'default_user.jpg', size: "35x35", :alt=>"user", :class=>"img-circle" %>
              </span>
              <%= current_user.username %><b class="caret"></b>
            <% end %>
            <ul class="dropdown-menu">
              <% if current_user.admin %>
                  <li><%= link_to rails_admin.dashboard_path do %><i class='fa fa-dashboard fa-fw'></i> Dashboard<% end %></li>
              <% end %>


              <li>
                <%= link_to fan_path(current_user.fan) do %><i class='fa fa-user fa-fw'></i> Profile<%end if current_user.fan%>
                <%= link_to artist_path(current_user.artist) do %><i class='fa fa-user fa-fw'></i> Profile<%end if current_user.artist%>
                <%= link_to executive_path(current_user.executive) do %><i class='fa fa-user fa-fw'></i> Profile<%end if current_user.executive%>
                <!--<%#= link_to admin_path(current_user.admin) do %><i class='fa fa-user fa-fw'></i> Profile<%#end if current_user.admin%>-->
              </li>
              <li>
                <%= link_to edit_fan_path(current_user.fan) do%><i class="fa fa-cog fa-fw"></i> Edit My Account<%end if current_user.fan%>
                <%= link_to edit_artist_path(current_user.artist) do%><i class="fa fa-cog fa-fw"></i> Edit My Account<%end if current_user.artist%>
                <%= link_to edit_executive_path(current_user.executive) do%><i class="fa fa-cog fa-fw"></i> Edit My Account<%end if current_user.executive%>
                <!--<%#= link_to edit_admin_path(current_user.admin) do%><i class="fa fa-cog fa-fw"></i> Edit My Account<%#end if current_user.admin%>-->
              </li>
              <li>
                <%= link_to edit_user_registration_path(current_user.id)  do%><i class="fa fa-key fa-fw"></i> Change Password<%end%>
              </li>
              <li class="divider"></li>
              <li>
                <%= link_to destroy_user_session_path, :method => :delete  do%><i class="fa fa-sign-out fa-fw"></i> Sign out<%end%>
              </li>
            </ul>
          </li>
      <% else %>
        <li class="innerLR">
          <%= link_to new_user_session_path do %>
              <button class="btn btn-success navbar-btn">Sign in</button>
          <% end %>
        </li>
        <li class="buttondivider"></li>
        <li class="dropdown">
          <%= link_to 'Join now <b class="caret"></b>'.html_safe, root_path, :class => 'dropdown-toggle', :data => {:toggle=>"dropdown"} %>
          <ul class="dropdown-menu">
            <li>
              <%= link_to 'Join as an Executive', new_executive_path %>
            </li>
            <li>
              <%= link_to 'Join as an Artist', new_artist_path %>
            </li>
            <li>
              <%= link_to "Join as a Music Lover", new_fan_path %>
            </li>
          </ul>
        </li>
          <%#= link_to "Sign up", new_user_registration_path %>  <%#= link_to "sign in", new_user_session_path %>
      <% end %>




      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>

